#include "main.h"
/**
 * mul - func checks forche cks for a digit (0 through).
 * @a: a - var
 * @b: b - var
 * Return: Allways 0.
 */
int mul(int a, int b)
{
	int mul;

	mul = a + b;
	return (mul);
}
