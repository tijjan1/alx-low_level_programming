#include <main.h>
/**
 * _ispper -prog checks for uppercase character
 * @c: Variable text
 * Retuurn: Always0 0.
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
